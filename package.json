{
  "name": "the-register-app",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix server",
    "start": "npm start --prefix server",
    "generate-vapid": "npm run generate-vapid --prefix server"
  }
}